type taimport, domain;
type taimport_exec, exec_type, vendor_file_type, file_type;
allow taimport default_prop:property_service set;
allow taimport vendor_file:file entrypoint;

init_daemon_domain(taimport)
